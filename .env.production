# Production server — loaded when APP_ENV=production
APP_ENV=production
APP_NAME=Payroll Accounting Mapper API

DATABASE_URL=postgresql://payroll:payroll@localhost:5432/payroll_mapper

# Comma-separated origins allowed by the API (the Next.js frontend)
CORS_ORIGINS=http://192.168.120.210:3000
